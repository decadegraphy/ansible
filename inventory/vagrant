# vim: ft=ansible_hosts:

# The port is subject to change if you have multiple Vagrant instances.
# Use `vagrant ssh-config` to retrieve the correct config if the following does not work for you.
vagrant ansible_host=127.0.0.1 ansible_user=ubuntu ansible_port=2222 ansible_ssh_private_key_file=.vagrant/machines/default/virtualbox/private_key
